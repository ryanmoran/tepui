---
provider:
  type: gcp
  params:
    credentials: some-credentials
    project: some-project
    region: some-region

environment:
  name: some-environment
  networks:
  - name: some-network
    cidr: 1.2.3.4/5
