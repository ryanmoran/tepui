---
provider:
  type: aws
  params:
    access_key: some-access-key
    secret_key: some-secret-key
    region: some-region

environment:
  name: some-environment
  networks:
  - name: some-network
    cidr: 1.2.3.4/5
